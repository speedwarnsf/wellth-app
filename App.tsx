import React from 'react';
import AppNavigator from './src/navigation/AppNavigator';

export default function App() {
  // Meta tags, fonts, and background are now set in web/index.html for proper SEO

  return <AppNavigator />;
}
